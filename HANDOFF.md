# Handoff — Phase 2 + Rejected-Categories Substantiation

*Written 2026-08-17 at the end of the site-build conversation; **updated 2026-08-17 (later session) after Task 0 and Phase 2A/2B/2C were built.** Sections 4–7 below are the original specs and are kept for the record; §1a records what was done and what is open.*

## 0a. START HERE (next session) — state as of 2026-08-18 evening, and the queue

**Read in order:** this section → §1d → §1c → research/CATEGORIES.md (decisions block) → research/REGISTRY.md → COPY_VOICE_GUIDE.md. Standing rules from memory: Opus subagents for fan-out; every final public sentence goes through Gemini Pro (`scripts/gemini-review.sh`, system prompts in `scripts/prompts/candidate-gemini-*-review-system.md`) and every Gemini claim is web-verified before it is applied (Gemini was wrong ~6 times today); Firecrawl approved server-wide for 403s; **new correction rule (2026-08-18):** verified factual errors are fixed in place anywhere in the corpus with one dated changelog line per file (git has the diff); "Review Additions" only for substantive additions; reviewer ledgers kept (`research/candidates/REVIEW_LOG_2026-08-18.md`, `research/RESEARCH_REVIEW_2026-08-17.md`).

**Where things stand:** 9 proposed amendments (Article 9 = 13th Amendment exception, 4 sections incl. a 2-year effective date; Term Limits now 4 sections with judicial retirement age at 75 and the confirmation-refusal default) · 21 candidate entries in the registry (11 standalone categories incl. age-limits-for-Congress and SCOTUS supermajority; clause variants under amendments) · 17 rejected (13 categories + 4 rejected clauses) — all in `site/src/content/data/registry.json` (49 entries) which drives `/candidates`, `/candidates/polling-questions`, `/rejected`, and the `LeftOut` lists on amendment pages. Site nav: Mission · Amendments · Candidates · Rejected · Get Involved · About; `/evidence/*` redirects. check-site passes; build 49 pages; **not deployed** since 2026-08-17.

**Founder decisions taken 2026-08-18 (evening) — do not re-litigate:** reject DC/PR statehood, filibuster, House expansion; fold judicial retirement age (75 ceiling, all Article III judges, Congress may set lower below SCOTUS) and the appointment-default knock-out clause (4–4 deadlock accepted and stated on the page) into Term Limits; accept Article 9 with the compulsion sentence kept (poll it as a split-sample arm) and a Section 4 two-year effective date; hold-and-poll every other candidate; three-bucket registry with twins merged; candidate-page band 700–1,100 words; correction-log rule above; **reopen the self-pardon ban as a candidate clause under Pardon Reform** (YouGov May 2026: 72% / 61% R / 85% D).

**Queue (in order):**
1. DONE 2026-08-18 evening: corrections cleanup (`scripts/prompts/corrections-cleanup.md`) — POLLING_RESEARCH.md + 27 files fixed in place, 68 inline correction bullets removed, SELECTION_METHOD §8/§9 + HANDOFF §2 rules updated. Article 9 Section 4 (two-year effective date) is on the page and in the registry.
2. DONE 2026-08-18 evening: self-pardon ban reopened as a candidate clause under Pardon Reform — `research/candidates/clauses/PARDON_SELF_PARDON_CLAUSE.md`, page `/candidates/self-pardon-ban` (Gemini-reviewed, 4 fixes applied), rejected page removed, registry/REGISTRY.md/CATEGORIES/IDEAS/POLLING/PARDON updated (YouGov May 2026: self-pardon 72/61R/85D; publication-with-reasons 79/79R/83D — POLLING gap #1 retired). Note for the editor: PARDON_RESEARCH.md's opening framing ('a self-pardon ban polls as an anti-Trump measure') was accurate for 2024 and is not supported by the 2026 reading — its Review Additions say so; the opening paragraph itself was not rewritten. check-site 694 footnotes ALL PASS; build 49 pages.
3. DONE — Gemini reviews applied; check-site and build pass as of the end of the 2026-08-18 session.
4. Michael's read of all `status: draft` pages (Article 9, Term Limits, three new rejected, age-limits, self-pardon; the registry-driven index pages) → flip status → **deploy** (`scripts/deploy.sh`, logged; may need `firebase login --reauth`) → verify on newbillofrights.net incl. the `/evidence/*` 301s.
5. Commissioned-polling next step: `/candidates/polling-questions` is now the survey spec — 41 blocks; the four Article 9 held-back clauses have path-back conditions but no drafted question yet (registry `pollQuestionNote`); the fold memos ask for a named-age poll (age limits, 70/75/80) and a forward-looking appointment-default poll.
6. **Salon outreach (2026-08-18, late):** plan at `research/SALON_OUTREACH_PLAN.md` (room design, four constituencies, phases toward an October 2026 first salon, ask ladder, invitation templates, 25%-floor balance rule) and verified target lists at `research/SALON_OUTREACH_TARGETS.md` (LA academics; centers/student chapters; politically active orgs on both sides; ethics/election bodies and alumni; bar sections, Fed Soc/ACS lawyer chapters, LA public-law firms; a "could not verify" list). Michael's decisions: no venue research; store results in the repo. Open: Michael's read; host-committee recruitment; the project one-pager and the privacy brief (public copy → Gemini review before use); the RSVP self-placement question.
7. Later/optional: build-time assertion that every registry `page` resolves to an MDX file; a scorecard page (all entries, one row each) if wanted; the filibuster/agency-independence candidate pages were never re-synced to the 1,100 band (filibuster is now rejected anyway; agency page could restore items its drafter cut).

## 1a. Status after the 2026-08-17 execution session

**Done (all pending Michael's manual review; every page still `status: draft`):**

- **Task 0A — research.** Ten memos in [research/rejected/](research/rejected/) (`<SLUG>_REJECTED.md`, ~2,000–2,900 words each, one template: proposal · evidence · gate result · framings tested · path back · sources). Every figure was web-verified by the drafting agent; the set was then reviewed by Gemini (`gemini-3.1-pro-preview`) and every reviewer note was verified on the web before anything was applied — verified additions/corrections sit in each memo's `## Review Additions (2026-08-17)`; unverified reviewer claims were dropped (e.g., its H.J.Res. 2 (2011) "correction" was wrong). Linked from research/README.md and CATEGORIES.md.
- **Task 0B — website.** `rejected` is now an MDX content collection ([site/src/content/rejected/](site/src/content/rejected/), schema in `content.config.ts`: title/order/rejectedOn/failedTest/summary/memo/status), with an index at `/evidence/rejected` (framing kept; one line per category naming the failed test) and a subpage per category at `/evidence/rejected/<slug>` (fixed structure: The proposal · The evidence · The test it failed · Framings we tested · The path back; `FullSpectrumChart` of the decisive number where one exists; GFM footnotes; every URL comes from the memo). `scripts/check-site.mjs` now checks the rejected pages (footnotes + URL provenance + banned words) and reads `research/rejected/*.md` as part of the corpus. Page conventions are recorded in COPY_VOICE_GUIDE.md.
- **Phase 2A — annotations.** `site/src/components/Note.astro` (`<details class="annotation">`, no JS); one `<Note>` after each of the 35 `**Section N.**` paragraphs, drafted from the memos' trap notes; Gemini feedback round run and 10 high-confidence accuracy/clarity fixes applied. Draft text unchanged (integrity check passes).
- **Phase 2B — polling page.** `/evidence/polling` is charts grouped by amendment (order = amendment order, then `pollingKeys` order) with the sortable table below (new Floor column). `FullSpectrumChart` no longer draws a threshold line; it labels each proposal's floor ("bipartisan floor · 71% (Republicans)" = lower of R/D) — global change (home, amendment pages, styleguide, polling, rejected pages).
- **Phase 2C — share images.** Build-time OG PNGs via `satori` + `@resvg/resvg-js` (`site/src/lib/og.ts`, fonts in `site/src/og/fonts/`, endpoints in `site/src/pages/og/`): one per amendment (`/og/amendments/<slug>.png`), per rejected category (`/og/rejected/<slug>.png`), per static page (list in `site/src/lib/ogPages.ts`), and `/og/default.png`; wired in `Base.astro` (`og:image`, `twitter:card`, `og:url`).
- **Corpus corrections found in the pass** (founder to apply; existing text untouched): appended to POLLING_RESEARCH.md as `## Review Additions (2026-08-17, rejected-categories pass)`. The most consequential: the 82% D / 43% R self-pardon figure is **YouGov Aug 15–19, 2024** (81/43/67), not Economist/YouGov Dec 2025 — the site's pardon page was corrected; CATEGORIES.md, PARDON_RESEARCH.md, SELECTION_METHOD.md, and AMENDMENT_IDEAS.md still carry the old attribution.
- Prompt files used for the agent runs are in [scripts/prompts/](scripts/prompts/) (templates: `rejected-memo-prompt.md`, `rejected-page-prompt.md`, `annotate-prompt.md`, `rejected-verify-prompt.md`).

**Open / for Michael:**

- Manual read of the ten rejected pages (nonpartisan discipline is the thing to check — abortion, voting rights, Electoral College especially), the 35 annotations, the polling page, and the share images; then flip `status`.
- Deployed to production 2026-08-17 (log `/tmp/newbillofrights-deploys/deploy-20260817-223652.log`); verified on newbillofrights.net (rejected index + subpages, polling charts, annotations, OG images all 200; still noindexed).
- Apply the flagged corpus corrections listed in POLLING_RESEARCH.md (self-pardon attribution in four docs; QI range 28–46%; balanced-budget pollster dates/splits; caution-list photo-ID split).
- Log the November 2026 abortion ballot measures (Idaho, Missouri, Nevada, Virginia) after the election (ABORTION memo).

## 1c. Candidate-categories pass (2026-08-18) — 16 memos + 16 pages, reviewed and re-synced; pending Michael's read + bucket decisions

Michael's instruction: research every candidate category to the same standard as the targets and rejects, evaluate all by the same criteria, and give candidates a home on the site. Also: use parallel Opus subagents for fan-out work and run all final public language by Gemini Pro (standing rule — see memory).

**Done:**

- **Research.** Fourteen memos in [research/candidates/](research/candidates/) (`<SLUG>_CANDIDATE.md`; template `scripts/prompts/candidate-memo-prompt.md` — the rejected-memo template plus §5 "what would resolve it, and the recommendation"; recommendations are to the founder, who decides). Every figure web-verified by the drafting agent. The eminent-domain memo (`EMINENT_DOMAIN_CANDIDATE.md`) came from a separate session the same day. Gemini review of the fourteen + web verification of every note: [research/candidates/REVIEW_LOG_2026-08-18.md](research/candidates/REVIEW_LOG_2026-08-18.md) (7 notes; 3 applied after verification, 2 reviewer errors — S.C. *did* ban fusion in 2022; S. 3096 has *no* Senate-inaction waiver — 1 permitted quotation, 1 tone fix).
- **Firecrawl retry queue.** Sources the agents could not fetch (congress.gov/GovTrack 403, Ballotpedia JS, several news sites, one Marquette crosstab, paywalls) are listed per memo in [research/candidates/FIRECRAWL_RETRY_QUEUE.md](research/candidates/FIRECRAWL_RETRY_QUEUE.md). Firecrawl credits ran out 2026-08-18 (reset ~08-20); Firecrawl is now allowed server-wide in `~/.claude/settings.json`. Retry, append verified items as Review Additions, and keep the rest in the queue.
- **CATEGORIES.md** — candidate entries now link their memos; a "Candidate memo recommendations (2026-08-18) — awaiting founder decision" table sits before the Rejected section. **Buckets were not changed** — moving DC/PR statehood to Rejected, folding age limits / appointment-default into Term Limits, etc., are Michael's calls. When he decides: move the entry, re-sync AMENDMENT_IDEAS.md / POLLING_RESEARCH.md / SELECTION_METHOD §10, and (for a rejection) write the rejected memo + page from the candidate memo.
- **Corpus corrections surfaced by the memos** *(all applied in place 2026-08-18 under the new rule — see §1d item 10a; this list is now historical)*: each memo's JSON `discrepancies` was recorded in the agent transcripts; the most consequential are also in the memos' §2 caveats and Review Additions. Notable: POLLING_RESEARCH.md's filibuster April 2021 "eliminate" figure is 19% not 16%; the Navigator filibuster item is March 2024 not Jan 2026; the immunity line (Marquette Feb 2025) is 68% R / 13% D favor, not 61/17; "impoundment is unpolled" is out of date (Marquette May 2025: 37% R floor); the '66% open data' Pew/Brookings figure is cumulative and hides a 45% R split; the FCC "61% R" abuse-framing floor is unsupported (Economist/YouGov item is 42% R); civil-service R support rebounded to 78% in the 2026 wave; the "~two-thirds want more parties" proxy should cite Pew/Gallup, not New America; the 13th-Amendment category has a party-split national poll (YouGov Oct 2024, 66% R) and fusion has a state one (FDU NJ 2023, 55% R).
- **Site.** `candidates` is now an MDX content collection (`site/src/content/candidates/`, schema `title/order/candidateSince/gate/summary/memo/status` — mirrors `rejected`); index at `/evidence/candidates`; subpage per candidate at `/evidence/candidates/<slug>` (fixed structure: The proposal · The evidence · Where it stands · Framings we tested · What would move it; no recommendation on the page); OG PNG per candidate (`/og/candidates/<slug>.png`); `scripts/check-site.mjs` covers the pages and reads `research/candidates/*.md` as corpus; cross-links from Home, Mission, Amendments index; page conventions in COPY_VOICE_GUIDE.md. `site/src/content/data/candidates.json` is superseded (delete once nothing references it). Nav label left as "Rejected Categories" per Michael's earlier choice — an "Evidence" hub is now the honest label; his call.
- **Prompt files** for reproducibility: `scripts/prompts/candidate-memo-*.md`, `candidate-page-*.md`, `candidate-verify-*.md`, `candidate-gemini-memo-review-system.md`, `candidate-gemini-page-review-system.md`.

**Pages (done, later the same day).** All 15 pages drafted from the memos (agents; the Opus fleet was killed repeatedly by an Anthropic API incident — 529 Overloaded — so on Michael's instruction the pages were drafted by Fable agents instead), then reviewed by Gemini Pro (`scripts/prompts/candidate-gemini-page-review-system.md`); applied: the "clears the bar honestly" phrasing removed from seven pages, civil-service floor corrected 94→95% and its "presidency did not change hands" sentence corrected (it did, Jan 2025), executive-lawfulness R range corrected to 27–37% (the 47 was the pro-power side) and "binds"→"applies", statehood tone/clarity edits. Gemini's "missing independent breakdown" notes were not applied (R/D is the rule; I is optional). Each page agent's `flags` list (things an editor should look at — mostly items cut for length) is in the agent transcripts under the session's tasks directory; the recurring one: pages sit at the top of the 550–850 band and several memo items were dropped for length. `node scripts/check-site.mjs` → 631 footnotes, ALL CHECKS PASS; `npm run build` → 48 pages incl. 15 candidate pages + 15 OG PNGs. **Not deployed** — Michael's read first (all `status: draft`).

**Later the same day (evening):**

- **SCOTUS-supermajority candidate** (Alex MacGregor's second idea) researched on Michael's instruction: `research/candidates/SCOTUS_SUPERMAJORITY_CANDIDATE.md` (unpolled; regime-coded proxies; the 2021 Presidential Commission and CRS treat a *statutory* supermajority rule as an open question, so why-an-amendment is weak) — Gemini review verified against the report text (Chapter 4, pp. 169–79; *Shelby County*/*Windsor* are federal-statute examples) — filed in CATEGORIES.md as candidate #16 with a page at `/evidence/candidates/scotus-supermajority`.
- **Firecrawl retry queue run** (credits reset the same day): 4 agents, ~120 scrapes, 25 items verified and folded into the memos; the queue file marks each item VERIFIED / STILL UNREACHABLE; details in `REVIEW_LOG_2026-08-18.md`. Gate-relevant: **fair districting's neutral-authority layer is now Strong** (NBC Aug–Sept 2025 national split 76% R / 92% D, n=30k) — memo, page, and the CATEGORIES table updated; House expansion's depth test "tested and failed" (11% R costs-named, 2006); fusion has a second NJ mechanism poll (51% R / 57% D, 2022); age-limits judicial half double-corroborated (Fox 2024 71% R).
- **13 pages re-synced** to the updated memos (one agent each; `scripts/prompts/candidate-page-resync-*.md`), then a second Gemini Pro pass over the 13 + SCOTUS; applied ~18 accuracy/symmetry fixes (e.g., 45% is Moderate not Weak; the DOJ existential sentence now states both Marquette 2026 readings; several one-party-only sentences balanced). `check-site`: 676 footnotes, ALL PASS; build 49 pages. Candidate pages now run ~950–1,150 words of body incl. headings (over the 550–850 rejected-page band) — Michael set the candidate-page band at 700–1,100 words (2026-08-18); recorded in COPY_VOICE_GUIDE and the page prompt. Optional follow-up: filibuster and agency-independence pages (the two not re-synced) still sit at ~850 with items their drafting agents cut for length — restore if wanted.
- Memo template no longer states a word target (depth over breadth).

**Open for Michael:**

- Read the 15 candidate pages (nonpartisan discipline; the hottest are statehood, DOJ independence, executive lawfulness, filibuster) → flip `status`, then deploy (`scripts/deploy.sh`, with logging).
- Bucket decisions from the CATEGORIES.md recommendations table (reject DC/PR statehood? fold age limits + appointment-default into Term Limits? etc.). Whatever moves: update CATEGORIES.md, AMENDMENT_IDEAS.md, POLLING_RESEARCH.md, SELECTION_METHOD §10; for a rejection, write the rejected memo + page from the candidate memo and delete the candidate page.
- Firecrawl retry queue (`research/candidates/FIRECRAWL_RETRY_QUEUE.md`) once credits reset (~2026-08-20).
- Nav: "Rejected Categories" vs an "Evidence" hub — Michael's call.
- **Three-bucket re-architecture (proposed 2026-08-18, Michael open to it; not started).** Make proposed / candidate / rejected uniform at BOTH levels — whole categories and the held-back clause variants inside greenlit amendments (today ~11 held-back clauses live only in memo prose + "What we left out"). Plan: (A) one registry (`kind: category|clause`, `parent`, `status`, floor/verdict, gate, memo, poll question + threshold; `statutory` as a tag on rejected; cross-refs so a rejected category and its held-back clause twin resolve to one entry) + short "variant candidate" memos for each held-back clause + a poll question each; (B) registry-driven pages, `/candidates/polling-questions` (the public survey spec — every candidate memo's §5 already writes the wording), "What we left out" rendered from the registry; (C) nav Amendments · Candidates · Rejected and `/evidence/*` → top-level URLs with redirects (site is noindexed, so cheap now). Open decision: merge clause twins into their category entry (recommended) or keep separate.
  - **Site re-architecture done: 2026-08-18 (part B/C of the plan above).** The site now renders the three buckets from a registry, and the evidence section moved to top-level URLs. **What changed:** (1) `registry` is a content collection in `site/src/content.config.ts` — `file()` loader over `site/src/content/data/registry.json`, zod schema per REGISTRY_INVENTORY §B with `.passthrough()` and nullable/optional fields throughout, so the file can be populated incrementally without breaking the build; every consuming page degrades to an empty state or its MDX fallback while the file is `[]`. (2) **URL moves** — `/evidence/candidates` → `/candidates`, `/evidence/rejected` → `/rejected`, `/evidence/polling` → `/polling` (and both `[slug]` routes); page files moved with `git mv`, every internal link updated across `site/src` (pages, layout, MDX prose in `term-limits.mdx`, three candidate pages), OG slugs renamed `evidence-*` → `polling`/`rejected`/`candidates` (the `path` field is what `ogImageForPath` matches, so the rename is cosmetic). Redirects in **both** places: `astro.config.mjs` `redirects:` emits 28 static redirect pages under `dist/evidence/**` (dynamic `[slug]` sources expand automatically from the destination collection), and `firebase.json` `hosting.redirects` carries six 301s with `:slug` globs, including a bare `/evidence` → `/candidates`. (3) **Nav** is Mission · Amendments · Candidates · Rejected · Get Involved · About; Polling added to the footer link list. (4) **`/candidates`** rebuilt registry-first with the MDX collection as fallback: "Standalone candidates" (kind category) and "Candidate provisions inside proposed amendments" (kind clause, grouped by parent with a link to `/amendments/<parent>`), `subItems` as nested bullets with their own gates. (5) **New page `/candidates/polling-questions`** ("Polling questions") — the public survey spec: what the wording is for and the rules for fielding it (keep the wording, report party crosstabs), then one block per candidate entry *and* per candidate `subItem` with the question as a pull-quote, follow-ups, threshold, and a link to its page or memo path; then "Path-back questions for rejected categories" from `pathBack`. Own OG image. (6) **`/rejected`** rebuilt the same way, framing kept, plus a "Rejected provisions inside proposed amendments" group. (7) **New `site/src/components/LeftOut.astro`** — takes an `amendment` slug, renders registry entries where `parent === slug || alsoListedUnder.includes(slug)` as a compact list with a status badge (Candidate / Rejected / In the text), the gate or failed test, `subItems` nested, and a link to the page (or the memo path as text when there is no page); proposed clauses are folded in and marked "now in the draft text, <section>". It renders nothing when the registry has no entries for that amendment. Inserted as `<LeftOut amendment="<slug>" />` at the top of `## What we left out`, directly under the "What we considered and set aside, and why:" line, in **all nine** amendment MDX files — `thirteenth-exception.mdx` landed from the parallel agent mid-task and got the insertion too. No other MDX prose was touched except the `/evidence/` link replacements. **Verified:** `node scripts/check-site.mjs` → 686 footnotes, ALL CHECKS PASS (it reads content dirs, not routes — no change needed); `npm run build` → **49 pages** plus the 28 `/evidence/**` redirect pages and 49 OG PNGs. Both were also run against a populated fixture registry (categories, clauses, twins via `alsoListedUnder`, `subItems`, `pathBack`) to confirm every branch renders; the fixture was deleted and `registry.json` is untouched at `[]`. **Follow-ups:** the registry file itself is the other agent's to populate — until it is, `/candidates` and `/rejected` fall back to the MDX collections (so clause groups and every `LeftOut` list render empty, and `/candidates/polling-questions` shows its empty state); once populated, re-read the three pages for duplicate wording between a registry `summary` and the MDX prose. `site/src/content/data/candidates.json` is still superseded and still unreferenced. The candidate/rejected MDX collections and their `[slug]` pages were deliberately left as-is, so a registry entry whose `page` points at a slug with no MDX file will render a dead link — worth a build-time assertion if the registry starts carrying pages that do not exist.
- Alex MacGregor's second idea (SCOTUS supermajority to strike statutes) — memo commissioned 2026-08-18 (`research/candidates/SCOTUS_SUPERMAJORITY_CANDIDATE.md`, filed in CATEGORIES.md as a candidate); page + Gemini review to follow.

## 1d. Bucket decisions + three-bucket build (2026-08-18, evening) — done; pending Michael's read, drafting decisions, deploy

**Michael's decisions (recorded in CATEGORIES.md):** reject DC/PR statehood, filibuster reform, House expansion; fold judicial retirement age + appointment-default into Term Limits; **accept the 13th Amendment exception clause as Target 9**; hold-and-poll everything else; three-bucket re-architecture, merging twins; candidate-page band 700–1,100.

**Done:**
- **Rejected conversions** — `research/rejected/{STATEHOOD,FILIBUSTER_REFORM,HOUSE_EXPANSION}_REJECTED.md` + pages `/rejected/<slug>` (orders 11–13); candidate pages removed; candidate memos retained with a "Superseded" header. Gemini-reviewed; fixes applied.
- **Term Limits fold** — PROPOSED_AMENDMENTS.md Article 2 now four sections (Section 2 gains the seat-clock / sits-as-constituted default; new Section 3 judicial retirement age at 75 with Congress able to set lower for lower courts; anti-evasion renumbered to Section 4); `TERM_LIMITS_RESEARCH.md` "Fold-in (2026-08-18)"; term-limits page updated (Notes, Why it can pass, If it passes, What we left out) and set back to `status: draft`. **Michael's drafting decisions:** (a) retirement age as a 75 ceiling with "by law" for lower courts (as drafted) vs a fixed number; (b) scope — all Article III judges (as drafted) vs Supreme Court only (the better-polled version); (c) accept the 4–4 deadlock as the stated cost of the knock-out rule.
- **Article 9 — Abolition of the Punishment Exception** — `research/THIRTEENTH_EXCEPTION_RESEARCH.md` (10k words, target template + Implementation section), draft text adopted into PROPOSED_AMENDMENTS.md (three sections; revision note lists the open choices), page `/amendments/thirteenth-exception` (36 footnotes, 3 Notes, `status: draft`), polling.json entry `thirteenth-state-measure`. **Michael's drafting decisions:** keep Section 2's compulsion sentence ("No person shall be compelled to labor by the infliction or threat of punishment") — the one clause beyond the literal polled proposition (recommended keep + poll as a split-sample arm); optional Section 4 prospective effective date (not adopted). Gemini-reviewed; six fixes applied (incl. Nevada is not a Republican-voting state).
- **Age-limits candidate** is now the congressional half only (page rewritten; memo header notes the fold). Appointment-default candidate page removed (folded).
- **Clause-variant memos** — 10 in `research/candidates/clauses/` (template `scripts/prompts/clause-memo-prompt.md`), each ending in a verbatim poll question; Gemini-reviewed and web-verified (`scripts/prompts/clause-verify.md`): 6 candidates (exec consequences, exec independent enforcement, emoluments remedy, pardon conspirators, lobbying lifetime ban, TL uniformity [drafting-counsel gate]) and 4 rejected clauses (pardon congressional disapproval — 30% R YouGov May 2026; privacy deletion right; ethics civil-service coverage — rejected by consensus 42R/37D; ethics forced divestment). Two Gemini "corrections" were wrong again (H.J.Res. 13 is 119th; Randolph's motion was 2–8–1).
- **Registry** — `site/src/content/data/registry.json` (49 entries: 11 proposed, 21 candidate, 17 rejected; 16 clauses; twins merged: fair-districting, executive-lawfulness, self-pardon-ban), mirror `research/REGISTRY.md`, inventory `research/REGISTRY_INVENTORY_2026-08-18.md`.
- **Site** — see the "Site re-architecture done" note above: `/candidates` (standalone + provisions by amendment), `/candidates/polling-questions` (public survey spec: 41 blocks), `/rejected`, `/polling`, nav Amendments · Candidates · Rejected, `LeftOut` component on all nine amendment pages, 28 redirects (Astro + Firebase 301). `check-site` 686 footnotes ALL PASS; build 49 pages. **Not deployed.**
- Docs re-synced: CATEGORIES.md (targets 9, decisions block, rejected +3), AMENDMENT_IDEAS.md (moves + §9), SELECTION_METHOD.md §10 update, research/README.md, COPY_VOICE_GUIDE (band, URLs).

**New evidence surfaced that Michael should act on** *(recorded under the then-current append-only rule; corrections are now applied in place — see §1d item 10a)*:
- **YouGov May 13–15 2026 Presidential Pardons survey (n=1,127)** — self-pardon ban **72% / 61% R / 85% D**: clears the ~60% R reopening threshold in SELF_PARDON_BAN_REJECTED.md §5 (revisability rule) — decide whether to reopen; publication-with-reasons **79% / 79% R / 83% D** — POLLING_RESEARCH gap #1 ("never directly polled") can be retired and the pardon page's Section 1 now has a Strong direct number; congressional reversal 30% R (used); family ban 57% R; impeachment-for-corrupt-pardon 54% R.
- Economist/YouGov Aug 14–17 2026: only 33% of Republicans call foreign gifts to any president unacceptable (82% D) — the emoluments remedy clause is regime-coded even in neutral wording.
- The ethics page's "no party breakdown published" for the all-federal-employees trading ban is wrong: UMD 2023 crosstabs exist (41.6R/37.1D/42.2I) — fix the footnote.
- POLLING_RESEARCH.md line ~157 still carries the uncorrected "lifetime ban 32%" figure; LOBBYING_RESEARCH.md's BLAST Act description misstates S. 4524's trigger and penalty text (see LOBBYING_LIFETIME_BAN_CLAUSE.md Review Additions).
- Registry noted two corpus inconsistencies it resolved in favor of the memos: redistricting floor (~55% R summary vs 70% R Marquette 2025 in the memo/page) and ethics (~40% summary vs 87% R stock ban).

**Open:** Michael's read of everything new (Article 9 page, Term Limits page, three rejected pages, age-limits page, `/candidates`, `/candidates/polling-questions`, `/rejected`, LeftOut lists) → flip `status` → deploy (`scripts/deploy.sh`, logged); the drafting decisions above; the self-pardon reopening; nav "Rejected Categories" → "Rejected" already changed per plan; `site/src/content/data/candidates.json` deleted (was superseded).

## 1b. Launch-gate session (2026-08-17, later) — E1–E3 done; E0 (indexing) deliberately NOT done

Michael's instruction: keep the site noindexed; do E1 QA, E2 favicons, E3 analytics. Also asked for: nav label change, contact@ alias check, repo security scan for going public, four logo samples.

**Done in this session:**

- **E1 Lighthouse (production):** before fixes — performance 100, accessibility 96–100, best-practices 96, SEO 54–63. Three findings fixed: `/amendments` heading order (cards now render `h2` under the page `h1` — `AmendmentCard` has a `headingLevel` prop); footnote back-reference links relied on color alone (now underlined); footer "learn more" link text (now "seed donor round"); the `favicon.ico` 404 console error is gone with E2. **After deploy: 100 / 100 / 100 (perf / a11y / best-practices) on mobile and desktop for every page tested** (home mobile perf 97); SEO 63 on every page, and the *only* failing SEO audit is `is-crawlable` — i.e., the noindex, which stays until E0.
- **E1 accessibility:** axe-core (WCAG 2.x A/AA + best-practice) run on all 32 built pages: **0 violations**. Static audit: every input labelled, every chart SVG has `role="img"` + full aria-label, skip link, `lang`, single `main`/`nav` landmarks. Added a global gold `:focus-visible` ring in `global.css` (was browser default).
- **E1 mobile:** 375 px viewport, every page: no horizontal overflow, no element wider than the viewport.
- **E1 dead links:** all 68 internal links resolve; 470 unique external URLs checked. ~60 hosts block automated requests (congress.gov, justia, canlii, thehill, ssrn, oecd…): every congress.gov bill URL was confirmed against GovTrack (bill exists), the rest were spot-checked by hand where possible. Two URLs are genuinely dead: `https://maint.loc.gov/law/help/elections/foreign-involvement/france.php` (DNS gone — LOC's maintenance host; the live URL is under `www.loc.gov`), and `https://www.cnn.com/2020/12/01/politics/pardon-investigation-doj/index.html` (404 — remove `/index.html`, or use the AP/Reuters equivalent). Also `harvardharrispoll.com/.../may-2025/` returns 500 and `news.gallup.com/file/poll/652532/...` 404. **Under the then-current append-only rule these were left for Michael to swap** (three were swapped in §1d item 2; corrections are now applied in place) (the check-site provenance rule means the page URL must match the corpus).
- **E1 forms (production):** mailing list (fetch/JSON path) ✔ 200 `{ok:true}`; seed donor (native POST path) ✔ 303 → `/get-involved/seed-donors?submitted=seed-donor`; honeypot ✔ silently dropped; invalid ✔ 400; GET ✔ 405. **Volunteer was ✘ 400** — the deployed `submitForm` predated the volunteer type (logs: `Expected 'mailing-list' | 'seed-donor'`), so the live volunteer form had been failing for everyone since it shipped. Functions redeployed this session → volunteer ✔ 200 `{ok:true}`. Three test docs to delete in the Firebase console: `mailingList/<sha256 of launch-qa-20260817@newbillofrights.net>` and the `seedDonorInterest` and `volunteerInterest` docs with that email (name "Launch QA (test)").
- **E2 favicons:** `scripts/make-favicons.mjs` (satori + resvg from the brand tokens: ink square, Caslon "N", gold mark) → `site/public/{favicon.svg,favicon.ico,favicon-32.png,apple-touch-icon.png,icon-192.png,icon-512.png,manifest.webmanifest}`; wired in `Base.astro` with `theme-color`.
- **E3 analytics:** Plausible snippet in `Base.astro` (`data-domain="newbillofrights.net"`, production builds only), live on production; Michael created the Plausible site. The post-deploy Lighthouse runs (real Chrome against newbillofrights.net) will already have registered pageviews.
- **Nav:** "Evidence" → "Rejected Categories" (Base.astro).
- **contact@newbillofrights.net** — first test message bounced ("Address not found"); Michael added the alias in Google Workspace during the session; second test delivered ✔.
- **Security / going public:** Snyk code (SAST) and dependency scans: 0 issues. No keys, tokens, or private keys in the tree or in git history (patterns: Google/AWS/GitHub/Slack/OpenAI keys, PEM). `.gitignore` covers `.env*`, `.firebase/`, `dist/`, `node_modules/`, `.astro/`. `gemini-review.sh` reads its key from `~/.claude.json` at run time (never embedded). Firebase project ID in `.firebaserc` and the hosting rewrite are safe to publish. **Decisions for Michael before flipping to public** (not made unilaterally): (1) `docs/conversations/` — 316 KB of Claude session transcripts (no secrets, but full internal deliberation, GCP project number, IAM commands); (2) references to the founder's other Google account/org; (3) no `LICENSE`. **Resolved 2026-08-18:** Michael keeps the transcripts and HANDOFF; the other-org references were scrubbed from tracked files and the archiver's redact list now covers them so nightly re-renders stay clean; LICENSE (MIT, code) + LICENSE-CONTENT (CC BY 4.0, research/copy) + README added.
- **Logo:** four samples in `design/logo-samples/` (generated by `scripts/make-logo-samples.mjs`); **Michael chose B, the monogram seal** (ink disc, gold ring, Caslon N, gold diamond) and it is now the site identity: `site/src/components/SealMark.astro` (inline, id-free SVG) in the header lockup and footer; favicons/app icons regenerated as the seal (`scripts/make-favicons.mjs` imports the geometry from `scripts/make-seal.mjs`); OG-image wordmark uses it (`og.ts`); `/styleguide` has a Logo section with usage rules; press asset at `/brand/seal.svg`. Deployed and verified 2026-08-17.

- **Deployed** hosting + functions to production 2026-08-17 (log `/tmp/deploy-logs/newbillofrights-deploy-*.log`); verified: favicons 200, Plausible present, nav label, footer link, volunteer form 200, noindex header + meta still in place.

**Still open for launch (E0 stays off until Michael says so):** copy sign-off → flip `status: draft`; swap the four dead source URLs (above); delete the three QA test docs; public-repo decisions (above); then remove noindex in both places (Base.astro meta + firebase.json header).

## 1c. Session 2026-08-18 — "After ratification" page + per-amendment implementation sections; candidates; licensing

- **New page `/amendments/after-ratification`** ("After ratification: what happens to the law on the books"): the general law of how a ratified amendment interacts with existing statutes — Article V mechanics, conflicting law becoming unenforceable but not erased (*Leser*, *Chambers*), self-executing text vs. enforcement clauses (*Jones*, *Katzenbach*, *Boerne*), anticipatory/conforming/trigger legislation (*Oregon v. Mitchell* → 26th; Pennsylvania's Liquor Control Board six days before repeal), effective-date/transition clauses, and litigation. Copy lives in `site/src/copy/after-ratification.mdx` (rendered by `site/src/pages/amendments/after-ratification.astro`); every fact/URL comes from the new memo `research/RATIFICATION_EFFECTS_RESEARCH.md` (all sources fetched and verified; congress.gov/constitution.congress.gov block fetchers, so LII/archives.gov are used). `scripts/check-site.mjs` now also checks `site/src/copy/*.mdx`. Linked from the amendments index, Mission, a new FAQ entry, and every amendment page; OG image added.
- **New section on all eight amendment pages — "If it passes: the law today and the day after"** (Federal law today · State law today · The day after), 430–455 words each, fully footnoted (429 footnotes site-wide now, was 324). Drafted by one agent per amendment from a shared prompt (`scripts/prompts/implementation-prompt.md`), each web-verifying every new URL and appending an `## Implementation: the law today and the day after (2026-08-18)` section to its category memo (a substantive addition, so it stays appended under the new rule; the memo sections are longer than the page sections and carry the litigation-question lists). Structure recorded in COPY_VOICE_GUIDE.md as section 5. One existing footnote corrected on the page: redistricting `[^kiley-bill]` said H.R. 4358 (that is the Texas member's Anti-Rigging Act); Kiley's bill is H.R. 4889 — fixed on the page; the memo's original bullet carried the wrong number and has since been fixed in place under the 2026-08-18 rule.
- **Draft-text questions the implementation pass surfaced** (for Michael — each is also in the memo's new §3(f); none was edited): *Term Limits* — §2 does not say whether justices sitting at ratification are counted/exempt or start an 18-year clock; no early-vacancy rule (remainder vs. fresh term); §1 counts elections not years (appointed Senate service uncounted). *Ethics* — no effective date, grandfather clause, or divestment window (sitting officers' holdings covered on day one); "covered officers" undefined in §§4–5; the leading Senate bill bans blind trusts while §1 guarantees them "as Congress shall by law provide." *Privacy* — no transition rule for data collected before ratification; §3 "except as provided by law" vs. Privacy Act "routine uses" (agency-published, not statutes). *Lobbying* — no effective-date/grandfather clause (people who left office before ratification; officials already registered as foreign agents); "senior position defined by law" is empty until Congress defines it; §1's only exceptions are own-behalf/uncompensated, so § 207's state/university exceptions may not survive *Boerne*. *Redistricting* — §3 read literally defers the article to the 2040 maps if ratification lands after Census Day 2030; court-drawn first map "established"?; consent decrees. *Executive Compliance* — no effective-date clause, so it reaches orders in force at certification (*Hollingsworth*); "officers of the United States" excludes employees and agencies-as-entities; remedy left entirely to existing statute. *Pardon Reform* — §§1–4 don't say "by the President"; §§1–4 say "reprieve or pardon" but §5 adds "commutations"; §1 names no publisher; §4 presupposes a bribery statute reaching the President. *Campaign Finance* — "elections" silent on ballot measures (*Bellotti*); §6 enforcement by "Congress and the States" vs. FECA preemption; whether struck-but-printed provisions revive without re-enactment.
- **Gemini Pro review pass (2026-08-18; Michael's standing rule: every piece of final public copy gets one).** Nine reviews (general page + eight sections). Applied: effective date is the last state's ratification, not the Archivist's certification (*Dillon v. Gloss*, verified, added to memo and page; "on certification" → "on ratification" across all sections); *McCutcheon* standard stated as "quid pro quo corruption or its appearance"; DISCLOSE Act example dropped from the campaign-finance section (one-party bill); lobbying state count rephrased (Public Citizen's categories overlap); pardon-immunity sentence hedged to what footnote 3 of the 2024 opinion actually says; idioms removed ("finish the job", "housekeeping", "hook", "works at once", "sketch", "flat rules", "stands … behind", "work by themselves"). Dismissed against verified sources: HONEST Act name (govinfo S. 1498) and the 119th-Congress bill numbers (govinfo). Card blurbs/ledes also reviewed — Gemini's notes are for Michael (he owns those strings): campaign-finance principleSummary is 32 words; privacy problemSummary is 26; pardon pair under 15; it flags "revolving door"/"highest bidder" and "self-serving" as idiom/emotive — its rewrites drift into officialese, so nothing was applied.
- **Facts the agents could not verify and therefore did not put on the site** are listed per amendment in the memo sections' sources/notes; the most useful to chase by hand: the House tally for H.R. 1526 (219–213), a 50-state count of legislators barred from owning individual stocks, and Virginia S.B. 688's final 2026 disposition.
- **New candidate category (Alex MacGregor): eminent-domain limits for private development (post-*Kelo*).** Registered in CATEGORIES.md, AMENDMENT_IDEAS.md, and the site's candidates list; research memo `research/candidates/EMINENT_DOMAIN_CANDIDATE.md` (agent, web-verified, three recommendation options — the deciding question is the existential-problem test, not polling). Also filed from Alex: the SCOTUS supermajority-to-strike-statutes idea (AMENDMENT_IDEAS) and two positioning ideas (new `research/MARKETING_IDEAS.md`: "ten amendments to mirror the Bill of Rights"; nonpartisan positioning).
- **Repo hygiene:** LICENSE (MIT, code) + LICENSE-CONTENT (CC BY 4.0, research/copy/design/archive) + README.md; other-org references scrubbed from tracked files, git history rewritten (`git filter-repo --replace-text`; pre-rewrite bundle at `/tmp/newbillofrights-pre-rewrite-20260818.bundle`), archiver redact rules added; the `origin` remote was removed by the rewrite — re-add under whichever GitHub owner the public repo will live in.
- **Identity:** the seal (logo B) adopted site-wide (header, footer, favicons, OG images, `/styleguide` Logo section).

## 1d. Session 2026-08-18 (later) — Michael's decisions executed

Michael's answers to the decision walk-through, and what was done:

1. **Copy sign-off:** every page flipped `status: draft` → `reviewed` (18 MDX files); the FAQ's "Draft answers" banner removed.
2. **Dead links:** three swapped to verified live URLs (LOC report PDF on tile.loc.gov; the CNN Dec 1, 2020 pardon-investigation story at its current slug; the Harvard CAPS/Harris May 2025 press release), each recorded in a `## URL updates (2026-08-18)` section of the memo; the Gallup topline PDF was never dead (HEAD 404, GET 200) — left as is.
3. **QA test docs:** all three deleted from Firestore (via the CLI account's token).
4. **Public repo:** pushed to **https://github.com/NewBillofRights/newbillofrights** (created **private**; flip to public in Settings when ready). `origin` now points there.
5. **Noindex:** kept, both places.
6–9. **Draft-text revisions (all eight articles)** — see the 2026-08-18 revision note at the top of PROPOSED_AMENDMENTS.md for the exact clauses: Term Limits §2 (early-vacancy rule; sitting Justices exempt); Ethics §1 ("covered officers" defined) + new §6 (one-year effective date and a one-year compliance window for later officeholders); Redistricting §2 ("by whatever authority the law provides") + §3 (keyed to the 2030 enumeration, voiding clause scoped); Pardon §§1, 2, 4 ("granted by the President"; President publishes) + §5 (commutations included); Lobbying new §5 (applies only to those who leave office after ratification); Privacy new §8 (prospective retention/use rule); Executive Compliance §1 ("the United States and all its officers"); Campaign Finance §5 (ballot measures for foreign money) + §6 (no revival of struck laws). Mirrored byte-for-byte to the site; annotations and "If it passes" sections updated; each memo carries a `## Draft revision note (2026-08-18)`. **Gemini Pro reviewed the revised clauses** and caught two real drafting problems that were fixed before deploy: the §3 voiding clause originally reached pre-2030 maps (now "no such district"), and Ethics §6 originally gave no window to *future* officeholders (now one year, Congress may shorten). Left open by design and still on the record: Term Limits §1 counts elections not years; Lobbying's "senior position defined by law" and the *Boerne* question on § 207 exceptions; Privacy §3 vs. routine uses; Campaign Finance §§1–4 and ballot measures (evidence supports foreign money only); Executive Compliance's remedy (statutory by design).
10. **Corpus corrections applied in place** (founder-authorized; the append-only rule was suspended for this task): 56 edits across ten memos, each line carrying `[corrected 2026-08-18; was: …]`, each file with an audit line atop its Review Additions section, and the "founder to apply" headings renamed "applied 2026-08-18". Not applied because there was no target text: SELECTION_METHOD §4.3/§10 and CATEGORIES.md carry the self-pardon 82/43 figure with no attribution (nothing to correct; the figures match YouGov Aug 2024 rounding).
10a. **Correction-log rule changed 2026-08-18.** Verified factual errors are now fixed **in place** anywhere in the corpus — no more append-only for plain factual corrections and no more "founder to apply" queues. Each edited file carries one dated changelog line near the top; git history holds the diff. Inline "Corrected: …" bullets and the "Flagged corrections — founder to apply" sections were removed once applied. Append-only `## Review Additions` sections are kept **only** for substantive additions (new evidence, changed gate results, reviewer disagreements). The reviewer ledgers (RESEARCH_REVIEW_2026-08-17.md, candidates/REVIEW_LOG_2026-08-18.md) stay as-is. **The pending corrections list was applied the same day** — POLLING_RESEARCH.md (16 fixes: the lobbying "32%" forced-choice misread, the missing House-expansion row, open-data 53% not 66%, the filibuster Monmouth/Navigator dates, the immunity and impoundment figures, the unsupported FCC "61% R" floor, the civil-service 2026 rebound, more-parties and fusion proxies with party splits, the redistricting floor at 70% R, the ethics floor at 87%, and Target 9 added), the self-pardon attribution (YouGov Aug 15–19, 2024 — not Economist/YouGov Dec 2025) in CATEGORIES.md and SELECTION_METHOD.md, the UMD all-federal-employees crosstabs (39.9%; 41.6% R / 37.1% D / 42.2% I) in ETHICS_RESEARCH.md and the site's ethics page, and the two BLAST Act misdescriptions in LOBBYING_RESEARCH.md.

11. **Card blurbs/ledes:** Gemini asked for three alternatives each; applied where they beat the current line on neutrality or concreteness and stayed faithful to the draft: Campaign Finance lede (25 words, was 31); Term Limits blurb ("Decades-long careers…") and lede (two terms / six terms / eighteen years); Lobbying blurb (drops "sell … to the highest bidder"); Pardon blurb and lede (drop "self-serving"/"pardons-for-sale"; name the three abuses the text bars); Privacy blurb trimmed to 24 words. Ethics, Executive Compliance, Redistricting kept as Michael wrote them.
12. **Eminent-domain staleness:** the only national poll with a party split is Saint Index / UMass Lowell, Oct–Nov 2005 (85 R / 79 D / 83 I) — twenty years old; the last national topline of any kind is Rasmussen 2014 (66%, no split); the ballot record runs 2006–2012 with no decay; the nearest recent reading is the Iowa Poll on pipeline takings, March 2023 (72 R / 82 D / 79 I). Held as a candidate pending a national re-poll (question wording in the memo §7(b)).
14. **Seed-donor email notifications** wired in `functions/src/index.ts` (nodemailer 9 via smtp.gmail.com:465, `SMTP_APP_PASSWORD` from Secret Manager via `defineSecret`, `SMTP_USER` default michael@newbillofrights.net, `NOTIFY_TO` default contact@; failures never fail the submission). Snyk clean. **Not yet deployed — needs the secret**: Michael runs `firebase functions:secrets:set SMTP_APP_PASSWORD` in his own terminal (it prompts; the value never touches chat or the repo), then `firebase deploy --only functions`. If the app password belongs to an account other than michael@newbillofrights.net, set `SMTP_USER` accordingly.
16. **Firebase/GCP project move — DONE 2026-08-18.** `newbillofrights-prod` now sits under the newbillofrights.net organization (`24513619171`, customer `C00max2wj`). Sequence that worked: both org-policy gates set (`allowedExportDestinations` on the source, `allowedImportSources` on the destination); Project Mover must be held on the project's *parent* (the source org), which the source org's DRS would not grant to an outside account — so the old account was given Project Mover on the source org and (after a temporary two-customer DRS on the new org) Project Creator on the new org, and ran the move. Cleanup done: temp grants removed, new-org DRS restored to `C00max2wj` only, both gate policies deleted, the old project-level DRS override deleted; site and function verified live after the move. **Still to do:** (a) `firebase login:add` as michael@newbillofrights.net, then `firebase login:use michael@newbillofrights.net` (interactive, Michael's terminal) so deploys stop depending on the old account; (b) billing needs nothing: the project's billing account `010A1E-E86EE7-28CC6C` already belongs to the newbillofrights.net org (confirmed in the console 2026-08-18); (c) then remove the old account's Owner role: `gcloud projects remove-iam-policy-binding newbillofrights-prod --member=user:michael@calloutbaddrivers.com --role=roles/owner`. Historical note (was): prepared — michael@newbillofrights.net now holds `roles/owner` + `roles/resourcemanager.projectMover` on `newbillofrights-prod`. Remaining steps are Michael's (destination-org credentials): see the session summary; in short — read the destination org ID as michael@newbillofrights.net, set the two org-policy constraints that gate cross-org moves (`resourcemanager.allowedExportDestinations` on the source org, `resourcemanager.allowedImportSources` on the destination), then `gcloud projects move newbillofrights-prod --organization=<NEW_ORG_ID>`; afterwards re-login the Firebase CLI as the foundation account, relink billing to an account under the new org, and remove the old account's Owner role.

## 0. Read first (in this order)

1. [research/MISSION.md](research/MISSION.md) — the mission and the full-spectrum test.
2. [research/SELECTION_METHOD.md](research/SELECTION_METHOD.md) — how categories are accepted/held/rejected; the evidence gate, failure modes, non-polling tests, traps catalog. **This is the framework for the rejected-categories task.**
3. [COPY_VOICE_GUIDE.md](COPY_VOICE_GUIDE.md) — hard rules for all public copy.
4. [WEBSITE_OUTLINE.md](WEBSITE_OUTLINE.md) — sitemap, architecture (§9), build plan (§10), decisions (§8).

## 1. Where things stand

- **Site is live at https://newbillofrights.net** (Firebase Hosting, project `newbillofrights-prod`, Blaze plan), **noindexed** (`X-Robots-Tag` header in `firebase.json` + `<meta name="robots">` in `site/src/layouts/Base.astro`; removing both is launch step E0). `www` redirects to apex. Forms (mailing list, founding-donor interest, volunteer interest — added 2026-08-17 at `/get-involved/volunteer`, collection `volunteerInterest`) work end-to-end via Cloud Function `submitForm` → Firestore.
- **All Phase 1 copy is written, sourced, and twice voice-reviewed**: 8 amendment pages (196 footnotes to primary sources), Mission, Home (text in [HOME.md](HOME.md)), About (founder bio), FAQ, salons. Every page still carries `status: draft` in frontmatter pending Michael's manual read.
- **Research corpus** (`research/`): 8 category memos + polling compendium, each with a `## Review Additions (2026-08-17)` section of web-verified additions; report at [research/RESEARCH_REVIEW_2026-08-17.md](research/RESEARCH_REVIEW_2026-08-17.md). Canonical draft text: [research/PROPOSED_AMENDMENTS.md](research/PROPOSED_AMENDMENTS.md) (revised 2026-08-17: CF Section 5 foreign money; Redistricting Section 2 state-court clause).
- Git: everything committed on `main`; last commit of the build conversation is the one adding this file.

## 2. How this repo works (conventions you must keep)

- **Deploy:** `scripts/deploy.sh prod` (or `preview`) — builds `site/` and deploys with a full persistent log under `/tmp/newbillofrights-deploys/`. Firebase CLI auth expires every few days; if a deploy fails with an auth error, ask Michael to run `firebase login --reauth`. Always verify a deploy by curling `https://newbillofrights.net/<path>` with `--resolve newbillofrights.net:443:199.36.158.100` (local DNS caches lag).
- **Integrity check before any content deploy:** `node scripts/check-site.mjs` — verifies draft text is byte-identical to PROPOSED_AMENDMENTS.md, footnotes are consistent, every URL exists in the research corpus, no banned words. Must print `ALL CHECKS PASS`.
- **Draft constitutional text is never edited in a copy pass.** It changes only in PROPOSED_AMENDMENTS.md (with a dated revision note) and is then mirrored verbatim into the page's `<div class="article-text">`.
- **Sourcing:** every factual claim on an amendment page has a GFM footnote `[^key]` → `[^key]: … — [Source](URL).` at the end of the file; every URL must appear in the research corpus (agents must never invent URLs). **Michael's rule (2026-08-17): footnotes cite facts and sources only — never narrate a correction** ("the widely quoted X was really Y" does not belong on the site).
- **Voice:** COPY_VOICE_GUIDE.md. House rules settled: ballot results are "wide margins" (never "landslides"); "clears the bar" is house vocabulary (keep); names in footnotes are fine (citations), never in prose as abuse examples; every polling number carries party breakdown + pollster + year (write "(no party breakdown published)" when the source has none).
- **Outside review:** `scripts/gemini-review.sh <system-file> <prompt-file>` calls `gemini-3.1-pro-preview` (Michael's chosen model; MCP config also switched). **Gemini is good at finding gaps and unreliable on facts — it fabricated URLs and "corrections" repeatedly. Never let a Gemini claim into the corpus or the site without fetching the source yourself.** Its rewrites drift into officialese; apply only high-confidence fixes.
- **Research corpus corrections are applied in place** (rule changed 2026-08-18; it used to be append-only). Once a factual error is verified at its source, fix the wrong number, date, or attribution wherever it appears — research memos, POLLING_RESEARCH.md, site footnotes — and add **one dated changelog line** near the top of each file touched: `*Changelog: YYYY-MM-DD — figures re-verified; N corrections applied in place (git history has the diff).*` Git history is the audit trail: no inline "Corrected: old → new" bullets, no "founder to apply" queues. **Appending is reserved for substantive additions** — new evidence, changed gate results, reviewer disagreements — in a dated `## Review Additions` section. The reviewer ledgers (`research/RESEARCH_REVIEW_2026-08-17.md`, `research/candidates/REVIEW_LOG_2026-08-18.md`) stay as written; they record which reviewer claims held up, not what the corpus now says.
- **Card blurbs / page ledes** = `problemSummary` / `principleSummary` in each amendment MDX frontmatter (Michael edits these himself; keep 15–25 words).
- **Parallel agents** work well here (one per amendment/doc, `general-purpose`, prompt file in `scripts/`); always give them the hard constraints above and require a JSON return.
- Firebase CLI is authenticated as the founder's other Google account (project owner also michael@newbillofrights.net). gcloud lives at `/opt/homebrew/share/google-cloud-sdk/bin/gcloud`.

## 3. Michael's decisions for this phase (verbatim intent, 2026-08-17)

- **Order:** rejected-categories substantiation FIRST, then Phase 2A/2B/2C.
- **2A annotations:** "proceed with draft as you see fit and feel free to name the trap the wording avoids." Then "run everything final by Gemini to get a round of feedback."
- **2B charts:** "don't draw a line at 50% and just label each proposal's floor number" (no floor line at all; label the floor — the lower of R/D — on every chart).
- **2C share images:** approved as designed.
- Footnotes: facts/citations only, no correction narratives (see §2).
- He will still manually review everything; keep `status: draft` until he clears pages.

## 4. Task 0 — Substantiate the rejected categories (do this first)

**Goal:** the ten rejected categories must be justified as completely as the eight targets are — evidence, sources, party breakdowns, the specific test each failed, and the path back — then the website updated accordingly.

**Inputs:** the Rejected list in [research/CATEGORIES.md](research/CATEGORIES.md) (dates + one-line reasons); [SELECTION_METHOD.md](research/SELECTION_METHOD.md) §4 (gate, evidence hierarchy, six failure modes) and §5 (three non-polling tests) and §10 ("Rejected (10) — and the test each failed"); the rejected-category sections retained in [research/POLLING_RESEARCH.md](research/POLLING_RESEARCH.md); [research/AMENDMENT_IDEAS.md](research/AMENDMENT_IDEAS.md) (ideas filed under rejected categories, kept for the record).

**The ten:** Abortion · Voting Rights · Electoral College & Presidential Elections · Supreme Court Expansion · Senate / Congressional Structure · Election Technology & Modernization · Civic Information & Informed Citizenry · Presidential Self-Pardon Ban · Eliminate Qualified Immunity · Balanced Budget.

**Deliverable A — research:** one memo per rejected category at `research/rejected/<SLUG>_REJECTED.md` (roughly a third the depth of a target memo), all to one template so they're comparable:
1. *The proposal as commonly framed* (and the main variants).
2. *The evidence*: best available polling with party breakdowns, pollster, fieldwork date, link (independent pollsters first; ballot results; legislative votes; advocacy polls flagged) — every number web-verified against the pollster's page.
3. *The gate result*: bipartisan floor per SELECTION_METHOD §4.1; which failure mode(s) in §4.3 apply, with the numbers that show it; which non-polling tests in §5 apply (e.g., civic information failed the existential test, not the polling test).
4. *Framings tested*: what alternative framings were considered and whether any clears the bar (e.g., QI "hold officers accountable" 79% / 64R; Electoral College "direct popular vote" 46R/80D).
5. *Path back*: the specific evidence that would reopen the category (a poll, a ballot result, a framing) — or "none identified."
6. *Sources* list.
Use parallel agents (one per category), require web verification of every number, and require the return JSON to list anything unverifiable. Then run the Gemini review + verify pass on the set (same discipline as RESEARCH_REVIEW).

**Deliverable B — website:** replace the current one-paragraph entries on `/evidence/rejected` with substantiated pages: recommend a subpage per rejected category (`/evidence/rejected/<slug>`, an Astro content collection `rejected` with MDX bodies drafted from the memos in COPY_VOICE_GUIDE voice, footnoted to sources, with a FullSpectrumChart of the decisive number where one exists) and an index that keeps the current framing ("These aren't judgments that the issues don't matter…") plus one line per category naming the failed test. Extend `scripts/check-site.mjs` to cover the new pages (footnote consistency + URL provenance; no draft-text check needed). Nonpartisan discipline matters *most* here — several rejected categories (abortion, voting rights, Electoral College) are the most partisan-live topics on the site; state the evidence and the test, never a view on the merits.

## 5. Task 1 — Phase 2A: annotated draft text

- **What:** each constitutional section on each amendment page gets an expandable plain-English note (NCC Interactive Constitution pattern), 2–4 sentences: what the section does in ordinary language, and **why it's worded that way — naming the trap the wording avoids** (drawn from the research memos' ⚠️ trap notes and design reasoning). 35 sections total across the eight articles.
- **Mechanics (recommended):** keep the `<div class="article-text">` markdown exactly as is (the integrity check depends on those `**Section N.**` lines) and add a small `Note` component after each section inside the div — `<Note>…</Note>` rendering `<details class="annotation"><summary>What this means</summary>…</details>` — no JS, `<summary>` marker reset already exists in `global.css`. Style it as a quiet aside within the document (smaller sans, ink-soft, gold hairline). Update `check-site.mjs` so `<Note>` blocks are ignored when comparing draft text (only `**Section` lines are compared, so this already holds — verify).
- **Process:** draft all 35 (agents, one per amendment, from the memos); then run each page's annotations through `scripts/gemini-review.sh` for a feedback round; apply high-confidence fixes only; verify; deploy; Michael reviews.

## 6. Task 2 — Phase 2B: polling page charts

- Replace the table-only `/evidence/polling` with a `FullSpectrumChart` per entry in `site/src/content/data/polling.json`, grouped by amendment (order by amendment `order`), **no floor line**; label each proposal's **floor number** (the lower of R/D) prominently on the chart (e.g., a small "floor: 71% (R)" caption/annotation); keep the sortable table below as the accessible view; sources already linked. Update the `FullSpectrumChart` component to accept `showFloorLine={false}` (or remove the line by default everywhere — check with the home page and amendment pages; Michael's instruction was global: "don't draw a line at 50%"). Re-run the dataviz palette check if colors change (they shouldn't).

## 7. Task 3 — Phase 2C: social share images

- Build-time OG image per page (1200×630): amendment title in Libre Caslon Text on the ink/parchment brand background, one-line summary in Public Sans, gold rule, wordmark. Tooling: `satori` + `@resvg/resvg-js` in an Astro endpoint or build step; fonts are in `site/public/fonts/`. Wire `<meta property="og:image">` in `Base.astro`. Provide a site-wide fallback image for non-amendment pages.

## 8. After Phase 2: launch gate (needs Michael's sign-off on copy first)

E0 remove noindex (both places) · E1 QA: Lighthouse ≥ 95, keyboard/screen-reader pass, mobile layout, dead-link sweep of all footnote URLs, forms retest on production · E2 favicons · E3 analytics (Plausible unless Michael says otherwise). Then Phase 3 items in WEBSITE_OUTLINE §10.

## 9. Open items waiting on Michael (don't block on these)

- Manual read of all pages → flip `status: draft` to `reviewed`.
- Google Workspace SMTP app password → enable seed-donor email notifications in `functions/src/index.ts` (TODO(D3)); until then submissions are read in the Firebase console.
- Luma link once the first LA salon has a date (salons page).
- Confirm Plausible for analytics.
- Two design questions from the research review are now decided and applied (foreign-money principle added; redistricting Section 2 fixed). Remaining flagged corrections in each memo's `### Flagged corrections` are his to apply.
- Longer-term: migrate the Firebase project out of the founder's other Google Cloud organization before incorporation.

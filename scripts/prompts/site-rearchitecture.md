You are re-architecting the New Bill of Rights Foundation site around three uniform buckets — Amendments (proposed) · Candidates · Rejected — driven by a registry. Repo: /Users/michaelhamilton/Documents/newbillofrights (Astro site in site/). Today is 2026-08-18. The founder approved this plan (HANDOFF.md §1c "Three-bucket re-architecture") and the merge-twins decision.

## Concurrency
- ANOTHER AGENT is writing site/src/content/data/registry.json right now (schema: research/REGISTRY_INVENTORY_2026-08-18.md §B — read it; a settled decision: twins merged with `alsoListedUnder`; `subItems`; rejected entries carry `pathBack`). The file currently contains `[]`. Do NOT write registry.json. Build everything to tolerate an empty array (render "no entries yet" gracefully) and to work when it is populated. If you need a fixture to test rendering, use a temporary copy under site/src/dev-fixtures/ that you delete before finishing.
- ANOTHER AGENT is creating site/src/content/amendments/thirteenth-exception.mdx and editing polling.json; leave those alone. Content MDX files (amendments/candidates/rejected) are NOT yours to edit except for the "What we left out" component insertion described below.

## Read first
site/src/content.config.ts · site/src/layouts/Base.astro (nav, ogImageForPath) · site/src/lib/ogPages.ts and site/src/pages/og/** · site/src/pages/evidence/** (candidates/index+[slug], rejected/index+[slug], polling.astro) · site/src/pages/amendments/** · site/src/components/ (FullSpectrumChart, AmendmentCard, Note) · site/astro.config.mjs · firebase.json · scripts/check-site.mjs (must keep passing) · COPY_VOICE_GUIDE.md ("Register", "Nonpartisan discipline", the three page-structure sections). Grep the whole site for '/evidence/' links.

## Build
1. **Registry collection**: in content.config.ts add `registry` = file('./src/content/data/registry.json') with a zod schema matching §B (use `.passthrough()`/optional fields liberally so a populated file validates; ids as `id`).
2. **URL moves** (site is noindexed, so cheap now): `/evidence/candidates` → `/candidates`, `/evidence/candidates/[slug]` → `/candidates/[slug]`, `/evidence/rejected` → `/rejected`, `/evidence/rejected/[slug]` → `/rejected/[slug]`, `/evidence/polling` → `/polling`. Move the page files (keep their content/structure and the MDX collections as-is), update every internal link in the site (pages, components, MDX prose links like `/evidence/candidates/age-limits` and `/evidence/rejected/court-expansion` — a grep-and-replace across site/src is fine), the OG endpoints (`/og/candidates/[slug].png` etc. can stay; ogPages.ts paths update), and add redirects BOTH in astro.config.mjs (`redirects: {'/evidence/candidates': '/candidates', '/evidence/candidates/[slug]': '/candidates/[slug]', ...}` — Astro's static redirect pages) AND in firebase.json `hosting.redirects` (301, with `:slug` globs) so old links resolve either way.
3. **Nav** in Base.astro: Mission · Amendments · Candidates · Rejected · Get Involved · About. Footer: add Polling if there is a footer link list.
4. **Candidates index** (`/candidates`): from the registry (fallback: the MDX collection if registry is empty): a short intro in the house voice (the current /evidence/candidates intro is good — keep its meaning: neither accepted nor rejected, waiting on specific evidence, same criteria); then two groups: **Standalone candidates** (kind category, status candidate) and **Candidate provisions inside proposed amendments** (kind clause, status candidate, grouped by parent amendment with a link to /amendments/<parent>) — each row: title, gate (kicker style), summary, link to page or parent page; subItems listed as nested bullets with their gates. Link to /candidates/polling-questions and /rejected and /polling.
5. **Polling-questions page** (`/candidates/polling-questions`, title "Polling questions"): the public survey spec. Intro: what it is (the exact wording each candidate is waiting on; forward-looking, both-parties, mechanism-specific; the floor it must clear; the follow-ups); a note that anyone commissioning or fielding these should keep the wording and report party crosstabs; then one block per candidate entry (and per subItem) — title, parent if a clause, gate, `pollQuestion.wording` (rendered as a quoted block), follow-ups as a list, threshold, and a link to its page/memo. Then a smaller final section **Path-back questions for rejected categories** listing rejected entries that have `pathBack`. Nonpartisan register; no advocacy. Empty-state text if no entries.
6. **Rejected index** (`/rejected`): keep the current content/framing but render from the registry (fallback MDX): categories, then **Rejected provisions inside proposed amendments** (kind clause, status rejected) with parent links.
7. **"What we left out" from the registry**: create `site/src/components/LeftOut.astro` that takes `amendment` (slug) and renders the registry entries where `parent === slug || alsoListedUnder.includes(slug)` and status ∈ {candidate, rejected} (and proposed clauses folded in, marked "now in the draft text, Section N") as a compact list: title · status badge (Candidate / Rejected / In the text) · gate or failedTest · link (page, or memo path text if no page). Insert `<LeftOut amendment="<slug>" />` at the top of each amendment page's `## What we left out` section, directly under the "What we considered and set aside, and why:" line, in all nine amendment MDX files (add the import next to the existing imports; keep all existing prose — the prose is the footnoted "why", the component is the tracked status). This is the ONLY MDX edit permitted; if thirteenth-exception.mdx does not exist yet, skip it and say so.
8. **Amendments index**: no structural change; make sure its cross-links use the new URLs.
9. Keep `scripts/check-site.mjs` passing (it reads content dirs, not routes) — but if it hardcodes any route, update it. Run `node scripts/check-site.mjs` and `cd site && npm run build`; every route in 2 must render; old `/evidence/*` routes must produce redirect pages. Report the build page count.
10. Update COPY_VOICE_GUIDE.md page-structure section headers to the new URLs (one-line edits), and append a dated note to HANDOFF.md §1c ("Site re-architecture done: …") listing what changed and any follow-ups.

Do not touch research/ files, registry.json, polling.json, or the amendments/candidates/rejected MDX beyond the LeftOut insertion and link URL replacements. Nonpartisan discipline in every new sentence.

Return ONLY this JSON:
{"routes":["..."],"redirects":<n>,"filesChanged":["..."],"leftOutInserted":["slugs"],"checkSite":"pass|fail: ...","build":"pass|fail: <n> pages","flags":["..."]}

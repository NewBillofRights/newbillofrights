You are building the REGISTRY for the New Bill of Rights Foundation — one JSON file listing every idea (whole category or clause variant) in exactly one of three buckets: proposed / candidate / rejected. Repo: /Users/michaelhamilton/Documents/newbillofrights. Today is 2026-08-18.

OUTPUT (overwrite; it currently holds `[]`): site/src/content/data/registry.json — a JSON array. Also CREATE research/REGISTRY.md: a short human-readable mirror (a table per bucket: id · title · kind/parent · floor/verdict · gate or failedTest · memo · page) with a one-paragraph header explaining the file is generated from registry.json and how twins are represented. Touch nothing else (another agent is concurrently building the site pages that read this file; do not edit content.config.ts, pages, or MDX).

## Schema
Use EXACTLY the schema in research/REGISTRY_INVENTORY_2026-08-18.md §B (read it in full), with these settled decisions: (1) twins are MERGED — one entry, `alsoListedUnder` carries the parent amendment slug(s), `supersedes` carries the retired clause ids; (2) `subItems` is used (not flat entries) for fair-districting (anti-favoritism floor / neutral authority), executive-lawfulness (appropriations fidelity / executive privilege / immunity), and exec-consequences (disqualification / pardon carve-out / salary suspension) — each subItem with its own gate and pollQuestion; (3) rejected entries carry `pathBack` {wording, threshold} where the memo names a specific poll, else `pathBack: null` with `pathBackNote`; (4) `pollQuestion.verbatim` true only when quoted from a memo; (5) add `summary` (one sentence, ≤35 words, nonpartisan, from the page frontmatter where a page exists) and `since`/`decidedOn` dates.

## Populate from (read all)
- research/CATEGORIES.md as of now (targets 1–9 incl. the 2026-08-18 decisions block; candidates; rejected incl. the three added 2026-08-18).
- research/PROPOSED_AMENDMENTS.md — nine articles; for proposed CLAUSES record `section` (e.g. "Term Limits, Section 3" for the judicial retirement age; "Term Limits, Section 2" for the confirmation-refusal default; Article 9 Sections 1–3 are the category itself, not separate clause entries).
- Inventory §A and §C (first-draft registry) — start from §C, then correct/complete it against the sources below (§C predates the decisions and the clause memos).
- The ten clause memos in research/candidates/clauses/*.md — status, floor, verdict, gate, the verbatim poll question(s) and threshold, decisiveNumber, memo path. Their statuses: exec-consequences candidate; exec-independent-enforcement candidate; ethics-emoluments-remedy candidate; pardon-conspirators candidate; pardon-congressional-disapproval (read the memo); lobbying-lifetime-ban candidate; term-limits-uniformity candidate (tag "drafting-counsel"); privacy-deletion-right rejected; ethics-civil-service-coverage rejected; ethics-forced-divestment rejected.
- The candidate memos research/candidates/*_CANDIDATE.md (§5 poll wording + thresholds; the current pages' frontmatter for summary/gate: site/src/content/candidates/*.mdx) — the live candidates are: age-limits (Congress only now), executive-lawfulness, fbi-doj-independence, civil-servant-independence, fair-districting, proportional-representation, good-government-data, fcc-independence, fusion-voting, eminent-domain, scotus-supermajority.
- The rejected memos research/rejected/*_REJECTED.md and pages site/src/content/rejected/*.mdx (13: the ten originals + statehood, filibuster-reform, expand-house) — failedTest, decisive number, path back.
- Folded clauses: judicial-retirement-age and appointment-default → kind clause, parent term-limits, status proposed, memo = the candidate memos, section per PROPOSED_AMENDMENTS.md; the age-limits candidate keeps only the congressional half.
- Page paths: use the NEW top-level scheme the site is moving to today: proposed → /amendments/<slug>; candidates → /candidates/<slug>; rejected → /rejected/<slug>; clause entries without their own page → page: null but `parentPage` "/amendments/<parent>".

## Rules
- Every number must exist in the cited memo/page; do not invent. Every URL verbatim from the corpus. Nonpartisan wording in titles/summaries (no side-coded vocabulary; pollster wording for positions).
- Validate: `node -e "JSON.parse(require('fs').readFileSync('site/src/content/data/registry.json','utf8'))"`; ids unique; every `parent`/`alsoListedUnder`/`supersedes` reference resolves to an existing id or amendment slug; every candidate has `gate` and (`pollQuestion` or `pollQuestion: null` with `pollQuestionNote`); every rejected has `failedTest`.

Return ONLY this JSON:
{"entries":<n>,"byStatus":{"proposed":<n>,"candidate":<n>,"rejected":<n>},"clauses":<n>,"twinsMerged":["..."],"candidatesWithoutPollQuestion":["..."],"flags":["..."]}

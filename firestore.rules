rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access is via the Admin SDK inside Cloud Functions.
    // No client ever reads or writes Firestore directly.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
